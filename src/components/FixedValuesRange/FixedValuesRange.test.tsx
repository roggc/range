
import React from 'react'
import { render } from '@testing-library/react'
import {FixedValuesRange} from '.'

describe('FixedValuesRange', () => {
    it('renders', () => {
      render(<FixedValuesRange />)
    });
  });
